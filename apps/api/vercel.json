{
  "version": 2,
  "buildCommand": "cd ../.. && npm run build -w packages/shared && cd apps/api && npx prisma generate && npm run build",
  "installCommand": "npm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
