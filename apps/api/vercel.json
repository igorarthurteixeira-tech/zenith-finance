{
  "version": 2,
  "buildCommand": "cd ../.. && npm run build -w packages/shared && cd apps/api && npx prisma migrate deploy && npx prisma generate && npm run build",
  "installCommand": "npm install",
  "functions": {
    "api/index.ts": {
      "maxDuration": 15
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
